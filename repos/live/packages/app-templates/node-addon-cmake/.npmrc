# Tells 'cmake-js' to use 'cmake -GXcode'.
# NOTE: 'cmake_G=Xcode' would probably also work, but maybe 'cmake-js' adjusts some settings when building an XCode project.
cmake_js_G="Xcode"

#cmake_js_runtime="electron"
