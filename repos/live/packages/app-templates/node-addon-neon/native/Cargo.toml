[package]
name = "node-addon-neon"
version = "0.1.0"
authors = ["Vaughan Rouesnel <vrouesnel@gmail.com>"]
license = "MIT"
build = "build.rs"
edition = "2018"
exclude = ["artifacts.json", "index.node"]

[lib]
name = "node_addon_neon"
crate-type = ["cdylib"]

[build-dependencies]
neon-build = "0.7.0"

[dependencies]
neon = "0.7.0"
