// We keep all paths in a separate file so its easier if we have to move things around.

export default {
  toolsDir: 'tools',
  reposDir: 'repos',
  templateMonorepoFiles: '../../template-monorepo-files',
}
