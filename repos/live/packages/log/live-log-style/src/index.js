export function spath(p) {
  return `'${p}'`
}

export function scode(p) {
  return `'${p}'`
}

export function spkg(p) {
  return `${p}`
}

export function sevt(p) {
  return `${p}`
}

// TODO(vjpr): Should we add formatters like milliseconds?
